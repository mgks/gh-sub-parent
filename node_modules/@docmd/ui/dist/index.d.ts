/**
 * --------------------------------------------------------------------
 * docmd : the zero-config documentation engine.
 *
 * @package     @docmd/core (and ecosystem)
 * @website     https://docmd.io
 * @repository  https://github.com/docmd-io/docmd
 * @license     MIT
 * @copyright   Copyright (c) 2025-present docmd.io
 *
 * [docmd-source] - Please do not remove this header.
 * --------------------------------------------------------------------
 */
export declare function getTranslationsDir(): string;
/**
 * Load translation strings for a locale. Falls back to English for missing keys.
 * Supports plugin overrides via the `overrides` parameter.
 */
export declare function loadTranslations(localeId?: string | null, overrides?: Record<string, string>): Record<string, string>;
/**
 * Create a `t()` function for use in templates.
 * Returns the translated string for a key, falling back to the key itself.
 * Supports simple interpolation: t('key', { name: 'value' }) replaces {name} in the string.
 */
export declare function createT(strings: Record<string, string>): (key: string, params?: Record<string, string>) => string;
/**
 * Clear the translations cache (useful for dev server rebuilds).
 */
export declare function clearTranslationsCache(): void;
export declare function getTemplatesDir(): string;
export declare function getAssetsDir(): string;
export declare function getTemplatePath(name: string): string;
export { resolveTemplate, clearTemplateResolverCache } from './template-resolver.js';
export type { TemplateSlot, TemplateHook, TemplateResolutionContext, ResolvedTemplate } from '@docmd/api';
