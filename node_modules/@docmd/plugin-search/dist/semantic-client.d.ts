/**
 * --------------------------------------------------------------------
 * docmd : the zero-config documentation engine.
 *
 * @package     @docmd/core (and ecosystem)
 * @website     https://docmd.io
 * @repository  https://github.com/docmd-io/docmd
 * @license     MIT
 * @copyright   Copyright (c) 2025-present docmd.io
 *
 * [docmd-source] - Please do not remove this header.
 * --------------------------------------------------------------------
 */
/**
 * Semantic search client module.
 * Handles vector-based semantic search using docmd-search.
 */
export {};
declare global {
    interface Window {
        DOCMD_SITE_ROOT?: string;
        DOCMD_ROOT?: string;
    }
}
export interface SemanticSearchContext {
    siteBase: string;
    ROOT_PATH: string;
    searchResults: HTMLElement;
    strings: {
        initial: string;
        noResults: string;
        error: string;
    };
    activeVersionFilters: Set<string>;
    globalAllVersions: string[];
    globalVersionColors: Record<string, {
        bg: string;
        fg: string;
    }>;
    selectedIndex: number;
    updateSelection: (items: NodeListOf<HTMLElement>) => void;
    showConfidence?: boolean;
}
/**
 * Load the semantic search index and client.
 */
export declare function loadSemanticIndex(ctx: SemanticSearchContext): Promise<boolean>;
/**
 * Perform semantic search and render results.
 */
export declare function performSemanticSearch(query: string, ctx: SemanticSearchContext): void;
