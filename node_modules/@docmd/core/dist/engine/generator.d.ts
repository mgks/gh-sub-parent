/**
 * --------------------------------------------------------------------
 * docmd : the zero-config documentation engine.
 *
 * @package     @docmd/core (and ecosystem)
 * @website     https://docmd.io
 * @repository  https://github.com/docmd-io/docmd
 * @license     MIT
 * @copyright   Copyright (c) 2025-present docmd.io
 *
 * [docmd-source] - Please do not remove this header.
 * --------------------------------------------------------------------
 */
interface RenderPagesOptions {
    config: any;
    srcDir: string;
    fallbackSrcDir: string | null;
    outputDir: string;
    hooks: any;
    buildHash: string;
    options: any;
    outputPrefix?: string;
    /** docmd core version (e.g. "0.8.7"). Used for the <meta name="generator"> tag. */
    coreVersion?: string;
    /** Progress callback: (current, total) called after each batch completes. */
    onProgress?: (current: number, total: number) => void;
    /** Optional: only render specific files (relative to srcDir). Used for incremental dev rebuilds. */
    targetFiles?: string[];
}
export declare function renderPages({ config, srcDir, fallbackSrcDir, outputDir, hooks, buildHash, options, outputPrefix, coreVersion, onProgress, targetFiles }: RenderPagesOptions): Promise<any[]>;
export {};
