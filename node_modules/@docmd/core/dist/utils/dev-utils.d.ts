/**
 * --------------------------------------------------------------------
 * docmd : the zero-config documentation engine.
 *
 * @package     @docmd/core (and ecosystem)
 * @website     https://docmd.io
 * @repository  https://github.com/docmd-io/docmd
 * @license     MIT
 * @copyright   Copyright (c) 2025-present docmd.io
 *
 * [docmd-source] - Please do not remove this header.
 * --------------------------------------------------------------------
 */
export declare const MIME_TYPES: Record<string, string>;
/**
 * Format an absolute path for display relative to CWD.
 */
export declare function formatPathForDisplay(absolutePath: string, cwd: string): string;
/**
 * Get the first non-internal IPv4 network address.
 */
export declare function getNetworkIp(): string | null;
export declare function getGitDevInfo(): {
    name: string;
    email: string;
    gravatarUrl: string;
};
export declare function getDevInfoScript(): string;
/**
 * Serve static files from rootDir with live-reload injection.
 */
export declare function serveStatic(req: any, res: any, rootDir: string): Promise<void>;
/**
 * Check if a port is in use.
 */
export declare function checkPortInUse(port: number): Promise<boolean>;
/**
 * Find the next available port starting from startPort.
 */
export declare function findAvailablePort(startPort: number): Promise<number>;
/**
 * Open a URL in the user's default browser. Best-effort: failures are
 * swallowed so the dev server is never taken down by a missing `xdg-open`
 * (e.g. inside the official Docker image, where the binary is not
 * installed). The call is also skipped entirely when running inside a
 * container — the official image sets DOCMD_CONTAINER=true for this.
 */
export declare function openBrowser(url: string): void;
