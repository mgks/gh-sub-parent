interface ThreadInfo {
    id: string;
    resolved: boolean;
    resolvedBy: string | null;
    resolvedAt: string | null;
}
interface CommentInfo {
    id: string | null;
    parentId: string | null;
    author: string;
    date: string;
    editedAt: string | null;
}
/**
 * Parse a thread info string.
 * Format: `<id> [resolved "<by>" "<date>"]`
 */
export declare function parseThreadInfo(info: string): ThreadInfo;
/**
 * Parse a comment info string.
 * Format: `[<id>] "<author>" "<date>" [reply-to <parentId>] [edited "<date>"]`
 */
export declare function parseCommentInfo(info: string): CommentInfo;
/**
 * Register all thread-related container rules on a markdown-it instance.
 */
export declare function setup(md: any): void;
export {};
//# sourceMappingURL=containers.d.ts.map