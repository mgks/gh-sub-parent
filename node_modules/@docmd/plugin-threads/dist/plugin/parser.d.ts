import type { Thread, Reaction } from '../types.js';
/**
 * Find the start and end line indices of the top-level `::: threads` block.
 * Returns null if no block found.
 */
export declare function findThreadsBlockBounds(lines: string[]): {
    start: number;
    end: number;
} | null;
/**
 * Parse a container block (lines between opening ::: and closing :::).
 * Returns array of child containers, each with { infoString, contentLines, startLine }.
 */
export declare function parseChildContainers(lines: string[]): Array<{
    infoString: string;
    contentLines: string[];
    startLine: number;
}>;
/**
 * Parse the info string of a thread container.
 * Format: `thread <id> [resolved "<by>" "<date>"]`
 */
export declare function parseThreadInfo(info: string): {
    id: string;
    resolved: boolean;
    resolved_by: string | null;
    resolved_at: string | null;
} | null;
/**
 * Parse the info string of a comment container.
 * Format: `comment <id> "<author>" "<date>" [reply-to <parentId>] [edited "<date>"]`
 * Legacy format (no id): `comment "<author>" "<date>" [edited "<date>"]`
 */
export declare function parseCommentInfo(info: string): {
    id: string | null;
    parent_id: string | null;
    author: string;
    date: string;
    edited_at: string | null;
} | null;
/**
 * Parse reactions from content lines inside a ::: reactions block.
 * Each line: `- <emoji> <author1>, <author2>, ...`
 */
export declare function parseReactions(lines: string[]): Reaction[];
/**
 * Parse a comment container's content lines into body and reactions.
 */
export declare function parseCommentContent(contentLines: string[]): {
    body: string;
    reactions: Reaction[];
};
/**
 * Parse threads from markdown content.
 */
export declare function parseThreadsFromContent(markdownContent: string): Thread[];
/**
 * Serialize threads to a ::: threads block string.
 */
export declare function serializeThreadsBlock(threads: Thread[]): string;
/**
 * Replace (or append) the threads block in a full markdown file.
 */
export declare function replaceThreadsBlock(markdownContent: string, threads: Thread[]): string;
//# sourceMappingURL=parser.d.ts.map